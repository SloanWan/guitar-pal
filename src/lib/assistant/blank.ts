/**
 * The blank in a suggested sentence — `___` — where the composer puts the
 * caret. Shared by every reader that offers sentences: the mark is the same
 * whichever assistant wrote the sentence.
 */
export const BLANK = "___";
